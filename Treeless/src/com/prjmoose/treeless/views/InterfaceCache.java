package com.prjmoose.treeless.views;

import java.awt.image.BufferedImage;

public class InterfaceCache {
	BufferedImage map;	// Only contains the rendered map for the current x, y, w, and h
	BufferedImage miniMap; // Only contains the rendered map
}
